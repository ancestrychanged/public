local cmds = {
	-- don't forget to add more cmds here if you're planning
	-- on making more cmds (inside the c server folder)
	messagebox = true,
	showimage = true,
	website = true,
	volume = true,
	shell = true,
}

return cmds
